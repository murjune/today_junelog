```python
# 구간 합 더하기

# 데이터 개수 N과 전체 데이터 선언

n = 5
data =[1,2,-3,4,5]

# 접두사 합 배열 계산

sum_value = 0
prefix_sum = [0]

for i in data:
    sum_value += i
    prefix_sum.append(sum_value);

# (i,j) 구간합 = prefix_sum[j] - prefix_sum[i-1]

# (3,4)구간 합 구하기
print(prefix_sum[4]-prefix_sum[2]) # 

```

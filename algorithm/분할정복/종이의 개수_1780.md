# 문제: 종이의 개수

# 풀이:

``` python


n = int(input()) # N(1 ≤ N ≤ 37, N은 3k 꼴)
result = [0,0,0]
graph = [list(map(int, input().split())) for _ in range(n)]



# 3*3 검사 함수
def inspect(x,y,n):
    tmp = graph[x][y]
    flag = True
    # 9
    for i in range(y,y+n):
        for j in range(x,x+n):
            if graph[i][j] != tmp:
                flag = False
                break

    if flag == True :
        result[tmp] +=1

    elif flag == False :
        if n == 3:
            for i in range(3):
                for j in range(3):

    n //= 3

        for i in range(0, 3):
            for j in range(0, 3):
                inspect(x+(n*i),y(n*j), n)



```
